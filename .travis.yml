language: python
python:
  - "2.7"

# command to install dependencies
install:
  - pip install -r requirements.txt

# command to run tests
script:
  - python manage.py test wom_classification
  - python manage.py test wom_pebbles
  - python manage.py test wom_river
  - python manage.py test wom_user
